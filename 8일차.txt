
indexOf() --> 호출한 String 객체에서 주어진 값과
	일치하는 첫 번째 인덱스를 반환합니다.
	일치하는 값이 없으면 -1을 반환합니다.

백준 4673번 이해가 안 됨.

charCodeAt() utf-16코드로 바꿔준다.

--------------------------------------

영어단어를 1 0 -1 .........

indexOf메소드 사용, String.fromCharCode()사용
join까지 사용.

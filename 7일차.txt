Math.max를 사용하면 최댓값을 찾아 준다.

(...sum)
...의 활용을 늘리자!



----------------------------------------

shift()메소드
배열에서 첫 요소를 제거하고 제거된 요소를 반환한다.



reduce 메소드!

배열의 합산을 구할때 사용
let avg = caseInput.reduce((acc, v)=> acc+= v*1, 0);

toFixed(idx) 메소드
소수점 idx자리까지 구해준다.






